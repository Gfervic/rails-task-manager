

<h1>Task list</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <h2> <%= task.name  %></h2>
    <h2> <%= link_to (task.name, task_path(id: task.id)  %></h2>

    <p> <%= task.priority %></p>
  </li>
  <% end  %>
</ul>

